export type SignInProps = {
  email: string;
  password: string;
};

export type RegisterProps = {
  name: string;
  email: string;
  password: string;
};

export type RefreshTokenProps = {
  refreshToken: string;
};
